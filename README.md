# Loki-Dhaka

Branch for secure QR PWA work: `feature/secure-qr-pwa-capacitor`

Environment
 - Copy `.env.example` to `.env.local` and fill your `NEXT_PUBLIC_*` Firebase values.
 - Add the repository secret `REACT_APP_LOKI_SECRET` (Actions → Secrets and variables → Repository) before merging.
